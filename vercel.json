{
  "ignoreCommand": "node -e \"const b=process.env.VERCEL_GIT_COMMIT_REF||'';if(b&&b!=='master'&&b!=='main'){process.exit(1)}\"",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
