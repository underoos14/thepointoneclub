# ============================================================
# THE POINT ONE CLUB — BRAND TOKENS
# ------------------------------------------------------------
# THIS IS THE ONLY PLACE brand colours, fonts and the shadow
# scale are defined. Site owners tweak values here; the layout
# and structural CSS stay untouched in src/index.css.
#
# palette
#   paper   — primary background (warm white)
#   surface — cards / panels
#   green   — secondary brand colour
#   red     — accent (CTAs, do/don't markers)
#   ink     — neutral dark gray text
# ============================================================

# Frontend URL
VITE_API_URL=
